<Window x:Class="DevBoxAI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:DevBoxAI"
        mc:Ignorable="d"
        Title="DevBoxAI - Android App Generator"
        Height="900" Width="1600"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top Bar -->
        <materialDesign:ColorZone Grid.Row="0" Mode="PrimaryMid" Padding="16">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <materialDesign:PackIcon Kind="Android" Width="32" Height="32" VerticalAlignment="Center" Margin="0,0,16,0"/>
                    <TextBlock Text="DevBoxAI" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
                    <Button Style="{StaticResource MaterialDesignFlatButton}"
                            Content="New Project"
                            Command="{Binding NewProjectCommand}"
                            Margin="8,0"/>
                    <Button Style="{StaticResource MaterialDesignFlatButton}"
                            Content="Open Project"
                            Command="{Binding OpenProjectCommand}"
                            Margin="8,0"/>
                    <Button Style="{StaticResource MaterialDesignFlatButton}"
                            Content="Settings"
                            Command="{Binding SettingsCommand}"
                            Margin="8,0"/>
                </StackPanel>
            </DockPanel>
        </materialDesign:ColorZone>

        <!-- Main Content Area -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400"/>
                <ColumnDefinition Width="4"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Chat Panel -->
            <Grid Grid.Column="0" Background="{DynamicResource MaterialDesignCardBackground}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="{DynamicResource PrimaryHueMidBrush}" Padding="16">
                    <TextBlock Text="AI Chat" FontSize="18" FontWeight="SemiBold" Foreground="White"/>
                </Border>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="8">
                    <ItemsControl ItemsSource="{Binding ChatMessages}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="8,4"
                                        Padding="12"
                                        CornerRadius="8"
                                        Background="{Binding Role, Converter={StaticResource RoleToColorConverter}}">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Role}" FontWeight="Bold" FontSize="11" Opacity="0.7"/>
                                        <TextBlock Text="{Binding Content}" TextWrapping="Wrap" Margin="0,4,0,0"/>
                                        <TextBlock Text="{Binding Timestamp, StringFormat='HH:mm:ss'}"
                                                   FontSize="10" Opacity="0.6" HorizontalAlignment="Right" Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <Grid Grid.Row="2" Margin="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox Grid.Column="0"
                             Text="{Binding CurrentMessage, UpdateSourceTrigger=PropertyChanged}"
                             materialDesign:HintAssist.Hint="Type your request..."
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             AcceptsReturn="True"
                             MaxHeight="120"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"/>

                    <Button Grid.Column="1"
                            Style="{StaticResource MaterialDesignFloatingActionButton}"
                            Command="{Binding SendMessageCommand}"
                            Margin="8,0,0,0"
                            Width="48"
                            Height="48">
                        <materialDesign:PackIcon Kind="Send" Width="24" Height="24"/>
                    </Button>
                </Grid>
            </Grid>

            <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Stretch" Background="{DynamicResource MaterialDesignDivider}"/>

            <!-- Preview and Code Area -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Toolbar -->
                <ToolBarTray Grid.Row="0" Background="{DynamicResource MaterialDesignCardBackground}">
                    <ToolBar Style="{StaticResource MaterialDesignToolBar}">
                        <Button Content="Preview" Command="{Binding ShowPreviewCommand}"/>
                        <Button Content="Code" Command="{Binding ShowCodeCommand}"/>
                        <Button Content="Files" Command="{Binding ShowFilesCommand}"/>
                        <Separator/>
                        <ComboBox ItemsSource="{Binding DeviceFrames}"
                                  SelectedItem="{Binding SelectedDeviceFrame}"
                                  Width="150"
                                  materialDesign:HintAssist.Hint="Device"/>
                        <ToggleButton Content="Dark Mode" IsChecked="{Binding IsDarkMode}"/>
                        <Separator/>
                        <Button Content="Build APK" Command="{Binding BuildApkCommand}" Style="{StaticResource MaterialDesignRaisedButton}"/>
                    </ToolBar>
                </ToolBarTray>

                <!-- Content Area -->
                <TabControl Grid.Row="1"
                            SelectedIndex="{Binding SelectedTabIndex}"
                            Style="{StaticResource MaterialDesignTabControl}">

                    <!-- Preview Tab -->
                    <TabItem Header="Preview">
                        <Grid Background="#F5F5F5">
                            <Border Background="White"
                                    Width="360"
                                    Height="720"
                                    CornerRadius="20"
                                    Effect="{StaticResource MaterialDesignShadowDepth3}">
                                <Grid Margin="8">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <!-- Status Bar -->
                                    <Border Grid.Row="0" Height="24" Background="#212121" CornerRadius="12,12,0,0">
                                        <Grid>
                                            <TextBlock Text="12:30" Foreground="White"
                                                       VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="10"/>
                                        </Grid>
                                    </Border>

                                    <!-- App Content -->
                                    <Border Grid.Row="1" Background="White" CornerRadius="0,0,12,12">
                                        <WebBrowser x:Name="PreviewBrowser" />
                                    </Border>
                                </Grid>
                            </Border>
                        </Grid>
                    </TabItem>

                    <!-- Code Tab -->
                    <TabItem Header="Code">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="4"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- File Tree -->
                            <TreeView Grid.Column="0" ItemsSource="{Binding FileTree}">
                                <TreeView.ItemTemplate>
                                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:PackIcon Kind="{Binding Icon}" Margin="0,0,4,0"/>
                                            <TextBlock Text="{Binding Name}"/>
                                        </StackPanel>
                                    </HierarchicalDataTemplate>
                                </TreeView.ItemTemplate>
                            </TreeView>

                            <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Stretch"/>

                            <!-- Code Editor -->
                            <Grid Grid.Column="2">
                                <TextBox Text="{Binding SelectedFileContent}"
                                         FontFamily="Consolas"
                                         FontSize="12"
                                         AcceptsReturn="True"
                                         VerticalScrollBarVisibility="Auto"
                                         HorizontalScrollBarVisibility="Auto"
                                         Padding="8"/>
                            </Grid>
                        </Grid>
                    </TabItem>

                    <!-- Files Tab -->
                    <TabItem Header="Project Files">
                        <ListBox ItemsSource="{Binding GeneratedFiles}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <materialDesign:PackIcon Grid.Column="0" Kind="File" Margin="0,0,8,0"/>
                                        <TextBlock Grid.Column="1" Text="{Binding Path}" VerticalAlignment="Center"/>
                                        <TextBlock Grid.Column="2" Text="{Binding Type}"
                                                   FontSize="10" Opacity="0.7" VerticalAlignment="Center"/>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </TabItem>
                </TabControl>
            </Grid>
        </Grid>

        <!-- Status Bar -->
        <materialDesign:ColorZone Grid.Row="2" Mode="PrimaryLight" Padding="8">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <materialDesign:PackIcon Kind="{Binding StatusIcon}" Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding StatusMessage}"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
                    <TextBlock Text="{Binding CurrentProject.Name, StringFormat='Project: {0}'}" Margin="0,0,16,0"/>
                    <ProgressBar IsIndeterminate="{Binding IsProcessing}"
                                 Width="100"
                                 Visibility="{Binding IsProcessing, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                </StackPanel>
            </DockPanel>
        </materialDesign:ColorZone>
    </Grid>
</Window>
